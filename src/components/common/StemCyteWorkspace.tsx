import { ArrowRight, CheckCircle2, CircleDashed, Database, FileSpreadsheet, FlaskConical, Link2, ShieldCheck, Snowflake } from 'lucide-react'
import { PageHeader } from './PageHeader'

interface StemCyteWorkspaceProps {
  eyebrow: string
  title: string
  description: string
  metrics: Array<{ label: string; value: string; note: string }>
  lanes: Array<{ title: string; detail: string; status: 'ready' | 'planned'; icon: 'lab' | 'data' | 'snow' | 'shield' }>
  nextAction: string
}

const icons = { lab: FlaskConical, data: Database, snow: Snowflake, shield: ShieldCheck }

export function StemCyteWorkspace({ eyebrow, title, description, metrics, lanes, nextAction }: StemCyteWorkspaceProps) {
  return <div className="flex flex-1 flex-col gap-5 overflow-auto p-5"><PageHeader title={title} description={description} /><div className="flex items-center gap-2 text-[10px] font-semibold uppercase tracking-[0.2em] text-muted-foreground"><span className="size-1.5 rounded-full bg-emerald-500" /> {eyebrow}</div><div className="grid gap-3 sm:grid-cols-3">{metrics.map((metric) => <div key={metric.label} className="rounded-2xl border border-border/70 bg-card p-4"><p className="text-xs text-muted-foreground">{metric.label}</p><p className="mt-2 font-heading text-2xl font-semibold tabular-nums">{metric.value}</p><p className="mt-1 text-[11px] text-muted-foreground">{metric.note}</p></div>)}</div><div className="grid gap-4 xl:grid-cols-[1.2fr_.8fr]"><section className="rounded-2xl border border-border/70 bg-card p-5"><div className="flex items-center justify-between"><div><p className="text-[10px] font-semibold uppercase tracking-[0.18em] text-muted-foreground">Operating lanes</p><h2 className="mt-1 font-heading text-lg font-semibold">StemCyte workflow readiness</h2></div><Link2 className="size-5 text-muted-foreground" /></div><div className="mt-5 space-y-3">{lanes.map((lane) => { const Icon = icons[lane.icon]; return <div key={lane.title} className="flex items-center gap-3 rounded-xl border border-border/60 p-3"><span className="flex size-9 shrink-0 items-center justify-center rounded-lg bg-muted"><Icon className="size-4" /></span><div className="min-w-0 flex-1"><p className="text-sm font-semibold">{lane.title}</p><p className="text-xs text-muted-foreground">{lane.detail}</p></div>{lane.status === 'ready' ? <CheckCircle2 className="size-4 text-emerald-500" /> : <CircleDashed className="size-4 text-amber-500" />}</div>})}</div></section><aside className="rounded-2xl border border-[#294653] bg-[#101a25] p-5 text-white"><p className="text-[10px] font-semibold uppercase tracking-[0.18em] text-[#9ec5d3]">Next operational move</p><h2 className="mt-2 font-heading text-xl font-semibold">{nextAction}</h2><p className="mt-3 text-sm leading-6 text-[#a9bbc4]">The workspace is ready for the source-of-truth connection. CRM/ERP records and branch laboratory CSV reports should arrive through versioned ingestion contracts so custody, patient, and test data remain traceable.</p><button className="mt-6 inline-flex items-center gap-2 text-xs font-semibold text-[#73d6c0]">Open integration mapping <ArrowRight className="size-3.5" /></button></aside></div><div className="grid gap-3 sm:grid-cols-2"><div className="flex items-center gap-3 rounded-xl border border-dashed border-border/80 bg-muted/20 p-4"><FileSpreadsheet className="size-5 text-emerald-600" /><div><p className="text-sm font-semibold">Laboratory CSV intake</p><p className="text-xs text-muted-foreground">Map branch test reports to CBU ID, accession ID, assay, result, reviewer, and report timestamp.</p></div></div><div className="flex items-center gap-3 rounded-xl border border-dashed border-border/80 bg-muted/20 p-4"><Database className="size-5 text-sky-600" /><div><p className="text-sm font-semibold">CRM / ERP handoff</p><p className="text-xs text-muted-foreground">Keep family, contract, payment, collection, inventory, and release events linked by a durable CBU identity.</p></div></div></div></div>
}
